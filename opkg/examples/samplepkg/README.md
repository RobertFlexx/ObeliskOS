# hello-sample package source

This directory can be turned into a `.opk` with:

```bash
opkg build examples/samplepkg examples/packages/hello-sample-1.0.0-x86_64.opk
```

The package payload comes from `rootfs/`.

## xinit package source

This package contains a real ELF `xinit` binary (not a compat wrapper).

Build:

```bash
./opkg/opkg build opkg/examples/xinit opkg/examples/packages/xinit-1.0.0-x86_64.opk
```
